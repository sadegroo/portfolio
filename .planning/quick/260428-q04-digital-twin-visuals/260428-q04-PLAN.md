---
quick_id: 260428-q04
slug: digital-twin-visuals
date: 2026-04-28
description: Add Simscape Multibody and variant-subsystem screenshots + pure-simulation video to BLDC post; commit re-encoded swingup/pushtest
mode: quick (manual; gsd-sdk not installed locally)
---

# Quick Task 260428-q04: Digital-Twin Visuals + Re-encoded Hardware Videos

## Goal

Make "The digital twin" section visually concrete (it's been prose-only). User dropped two perfect screenshots (Simscape Multibody plant + the variant subsystem with SIMULATION/PHYSICAL SYSTEM labels) plus a simulation video showing the pure-simulation experience end-to-end. Wire all three into the post. Also pick up the re-encoded `swingup.mp4` and `pushtest.mp4` (smaller and uniformly sized for visual consistency).

## Source assets (all in `content/posts/bldc-pendulum/`)

- `simulink_multibody_model.png` (119 KB) — Simscape Multibody block diagram of the pendulum plant.
- `simulink_variant_blocks.png` (141 KB) — the variant subsystem with two branches labelled "SIMULATION" / "PHYSICAL SYSTEM" — literally the figure the post's title alludes to.
- `simulink_simulation.mp4` (**33 MB** — see Notes) — pure-simulation run: button → swingup, Mechanics Explorer mirrors, setpoint moved twice, drop.
- `swingup.mp4` (re-encoded, 583 KB, was 2.9 MB) — same content, smaller.
- `pushtest.mp4` (re-encoded, 3.4 MB) — re-encoded for consistency with swingup.

## Tasks

1. **Restructure "The digital twin" section** (line 106). Split the single dense paragraph in two:
   - Para A: model contents (E-L equations, UKF, controllers) + new sentence introducing the Multibody plant. Insert `simulink_multibody_model.png` figure.
   - Para B: variant-subsystem mechanics (current sentence, slightly tweaked: "nonlinear plant block" → "Multibody plant"). Insert `simulink_variant_blocks.png` figure.
2. **Add simulation video block in "What it looks like running"** (after pushtest, before "If you want to play..." paragraph). Add a one-line transition that names what the video shows (swingup trigger / Mechanics Explorer / two setpoint nudges / drop). Lightly tweak the existing "If you want to play..." paragraph to read as a follow-up to the video rather than a setup.
3. **Captions on all three new figures/videos** — terse, descriptive, matching the existing tone. No over-claiming.
4. **No code/asset filename changes** for `swingup.mp4` and `pushtest.mp4` — index.md already references them by lowercase name; git tracks them lowercase; the on-disk Windows filenames being `Swingup.mp4` is a Windows case-insensitivity artifact that doesn't propagate to commits.
5. **STATE.md** — Last activity refresh; record q04; flag the 33 MB sim video as a future-cleanup candidate.
6. **Commit atomically.**
7. **Do NOT push** — user specifically OKs each push, and the 33 MB video should be re-encoded or replaced with a YouTube embed before going live.

## Constraints

- Don't push.
- Tone preserved (terse, factual, lightly personal).
- Captions describe what's IN the image — don't make extra technical claims.
- All `<video>` tags use `width="100%"` for visually consistent player width.
