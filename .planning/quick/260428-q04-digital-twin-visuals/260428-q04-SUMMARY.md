---
quick_id: 260428-q04
slug: digital-twin-visuals
date: 2026-04-28
status: complete
---

# Quick Task 260428-q04 — Summary

## What changed

`content/posts/bldc-pendulum/index.md`:
- **"The digital twin" section restructured.** The dense single paragraph at line 106 split into two: model contents (now ending with a sentence that introduces the Simscape Multibody plant) → `simulink_multibody_model.png` figure → variant-subsystem paragraph (with one prose tweak: "nonlinear plant block" → "Multibody plant") → `simulink_variant_blocks.png` figure.
- **Pure-simulation video added in "What it looks like running"**, after the push-test figure. New transition sentence sets up what the video shows (swing-up trigger, Mechanics Explorer, two setpoint nudges, drop). Existing "If you want to play..." paragraph rewritten to "If you want to try it yourself..." — flows as a follow-up to the video instead of a setup for it.
- All three new figures/videos use the same patterns as existing ones: `{{< figure >}}` shortcode for stills; `<figure>/<figcaption>` HTML wrapping `<video controls preload="metadata" width="100%">` for videos.

`content/posts/bldc-pendulum/`:
- `simulink_multibody_model.png` (119 KB) — committed.
- `simulink_variant_blocks.png` (141 KB) — committed.
- `simulink_simulation.mp4` (588 KB) — committed. Initial encode was 33 MB; user re-encoded before this commit hit origin and the bloat-carrying intermediate commit was squashed out of local history (`git reset --soft HEAD~2` + recommit) so origin only ever sees the small version.
- `swingup.mp4` re-encoded (583 KB, down from 2.9 MB).
- `pushtest.mp4` re-encoded (3.4 MB, similar to before — sized to match swingup visually).

## Notes / Loose ends

- **Page weight resolved.** Initial sim-video encode was 33 MB; user re-encoded to 588 KB (matching `swingup.mp4`'s 583 KB). To keep the 33 MB blob out of origin permanently, the two intermediate commits (`72d5e59` digital-twin visuals + `1c86dee` recompression) were squashed locally via `git reset --soft HEAD~2` + recommit before push. Origin only ever sees the small version.
- **Filename case sanity-check passed.** `Swingup.mp4` on disk (Windows case-insensitive) is tracked by git as `swingup.mp4` (lowercase) — confirmed via `git ls-files`. Linux CI will see the lowercase name, which matches the markdown `src` attribute.
- **Multibody-vs-Euler-Lagrange prose nuance.** The post previously said "the model contains the nonlinear equations of motion (derived symbolically with Euler-Lagrange...)" and then talked about the "nonlinear plant block." The new prose clarifies that the *plant* (what the controller sees in simulation) is a Simscape Multibody model — joints + transforms + rigid bodies — while the symbolically-derived E-L equations live in the controller / Kalman filter side of the model. This was implicit before and is explicit now. If this is wrong (e.g. you actually use the symbolic E-L model as the plant too), let me know and I'll adjust.

## Phase 5 status after this task

Sim-vs-hardware overlay (`sim-vs-hardware.png`) is now the *only* remaining commented-out placeholder in the post. The architecture-animation placeholder is marked optional. Phase 5 effective progress: 4 of 4 originally planned visuals shipped (counting the simulation video as part of the digital-twin story even though it wasn't in the original Phase 5 scope), plus two unplanned bonus images and a re-encode pass. Time to look at reconciling Phase 5 success criteria against what actually shipped, then closing it out.

## Verification

- Hugo not built locally as part of this change.
- New figure shortcodes match the working pattern (`{{< figure src="..." alt="..." caption="..." >}}`); new video block matches the working pattern (`<figure><video.../><figcaption>...</figcaption></figure>`).
- Visual sizing: all three videos use `width="100%"`. Aspect ratios may differ (hardware videos vs. screen-capture sim video) so heights might not match exactly. Visual check on `hugo server` recommended before push; if heights look jarring, can constrain with a max-height in CSS or by re-encoding to matched aspect ratios.
