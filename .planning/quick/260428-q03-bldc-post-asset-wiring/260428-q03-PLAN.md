---
quick_id: 260428-q03
slug: bldc-post-asset-wiring
date: 2026-04-28
description: Wire up two BLDC post assets (hardware photo, push-test video) and a copyedit
mode: quick (manual; retroactive — work was done freeform, plan written after the fact for trail completeness)
covers_commits:
  - ffa66a6  # docs(post): add BLDC rig photo and fix garbled push-test sentence
  - 8bd39a9  # docs(post): add push-test video and caption both clips
related: 260428-q02 (the dual-rig rewrite this builds on)
---

# Quick Task 260428-q03: BLDC Post — Asset Wiring (Hardware Photo + Push-Test Video)

## Status note

**This plan is retroactive.** Both commits below were made freeform during a working session immediately following q02, without a `.planning/quick/` record. The work was small, atomic, and on-trend with q02 — but the GSD enforcement in CLAUDE.md says all changes go through GSD. Documenting after the fact preserves the audit trail.

## Goal

Wire up two assets the user dropped into the BLDC post page bundle (`hardware_picture.png` and `pushtest.mp4`), give both videos in the post real captions, and fix one garbled sentence the user flagged. These advance Phase 5 (BLDC Visuals) from the v2 ROADMAP — but were executed as quick tasks, not as a planned Phase 5 plan.

## Tasks (as executed)

### Commit ffa66a6 — hardware photo + sentence fix

1. Commit `content/posts/bldc-pendulum/hardware_picture.png` (the user dropped it untracked alongside the other bundle assets).
2. Replace the commented-out `rig-overview.jpg` figure shortcode placeholder in `index.md` with an active `{{< figure >}}` referencing `hardware_picture.png`. Caption: *"The BLDC rig, assembled and at rest."* Alt: describes the assembled stack (NUCLEO + IHM08M1, maxon brushless on top, pendulum hanging).
3. Fix garbled sentence in "What it looks like running" — original was *"the moment a push-test pendulum shoves over and the controller catches it just before commitment"* (subject/object inverted, opaque ending). New: *"the moment a push test shoves the rod off vertical and the controller catches it before it falls."*
4. Drop "rig wide shot" from the bottom-of-post TODO list.
5. STATE.md: drop 📷 Rig wide shot from Open TODOs.

### Commit 8bd39a9 — push-test video + captioning both clips

1. Rename `Pushtest.mp4` → `pushtest.mp4` via two-step trick (Windows is case-insensitive; Linux CI is not, so the case must match the markdown reference). Commit the renamed file.
2. Wrap both videos in `<figure>/<figcaption>`:
   - Swing-up: *"Swing-up: rest to upright, then hold."*
   - Push test: *"Push test: a finger tap, a harder one, and finally one shove too many."* (Caption teases the controller failing at the end; user explicitly asked for this so people watch.)
3. Add a one-sentence transition between the two clips: *"The swing-up is the textbook trick. The honest question is what happens once you start poking — and how hard you can poke before the LQR runs out of authority."*
4. Replace the stale `🎥 PUSH RECOVERY CLIP` brief comment (which assumed clean recovery) with a `🎥 PUSH TEST CLIP` brief that matches what was actually shot — controller failure included, on purpose. Filename in the brief updated to `pushtest.mp4`.
5. Drop the push-recovery item from the bottom-of-post TODO list.
6. STATE.md: drop 🎥 Push-recovery clip from Open TODOs.

## Constraints (honored)

- No push (per CLAUDE.md).
- Tone preserved (terse captions, lightly personal transition).
- Stale "push recovery" framing replaced because the actual clip ends in failure — re-using the old brief verbatim would have been dishonest.
- Lowercase filename for `pushtest.mp4` to match the existing `swingup.mp4` convention and to deploy correctly on Linux CI.

## What Phase 5 progress this represents

Phase 5 (BLDC Visuals) success criterion 1 originally listed four target assets:
- ✅ `rig-overview.jpg` → shipped as `hardware_picture.png` (this task)
- ❌ `sim-vs-hardware.png` → **still pending**
- ✅ `competition-leaderboard.png` → shipped as `scoring_theta.png` + `scoring_pi.png` (q01)
- ✅ `push-recovery.mp4` → shipped as `pushtest.mp4` with reframed "push test" narrative (this task)

Phase 5 is effectively 3/4 complete — but the ROADMAP success criteria still name the *originally planned* filenames. Reconciling the criteria against actual shipped filenames is its own task; flagged in the SUMMARY for follow-up.
