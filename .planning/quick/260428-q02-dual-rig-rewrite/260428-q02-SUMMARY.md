---
quick_id: 260428-q02
slug: dual-rig-rewrite
date: 2026-04-28
status: complete
---

# Quick Task 260428-q02 — Summary

## What changed

`content/posts/bldc-pendulum/index.md`:
- **Frontmatter**: `tags` gains `stepper`; `summary` rewritten to lead with the framework, not the BLDC drive.
- **Intro (line 40)**: "brushless motor's shaft" → "motor shaft" — sentence one is rig-neutral.
- **Setup section** (renamed `## The setup: one kit, two rigs`): rewritten to lead with the shared STEVAL-EDUKIT01 kit + NUCLEO-F401RE + 2400 CPR pendulum encoder, then introduce both motor variants. Added a 6-row contrast table covering motor / driver board / control rate / motor encoder / pendulum encoder / firmware repo.
- **Brain and muscle / STM32 paragraph**: generalized — covers both rigs in one paragraph (BLDC: 16 kHz FOC torque on IHM08M1; stepper: 1 kHz acceleration command, L6474 handles current in hardware).
- **Digital twin / variant-subsystem paragraph**: added the explicit title clarifier ("Those are the two targets in the title — *simulation* and *real hardware*, from one model file") and a parenthetical noting the stepper-vs-BLDC choice is a second variant switch layered underneath.
- **Teaching lab**: scoring figures now captioned and alt-tagged "(stepper rig)"; analysis paragraph leads with "two teams ran the stepper rig"; closes with one sentence noting the BLDC rig produces the same shape of plot.
- **What it looks like running**: added "(the BLDC variant, in this clip)" parenthetical to disambiguate the swing-up video.
- **Try it / source**: split firmware bullet into BLDC variant + stepper variant; added `sadegroo/invpend_stepper` link with the same TODO marker about confirming visibility before publishing; pluralized the build-instructions follow-up sentence.
- **Bottom TODO list**: dropped "leaderboard" from item 4 (handled in q01); changed "both" → "all three" in item 1 to match the new third repo.

`.planning/STATE.md`:
- Added `260428-q02` row to Quick Tasks Completed.
- Recorded q01's commit hash (`fd5ff30`) — was `_pending_`.
- Added a new "Decisions (added 2026-04-28, dual-rig rewrite)" block under Accumulated Context. Captures three load-bearing decisions: post covers both rigs; title kept with body clarification; stepper repo listed alongside BLDC. Goal: prevent future edits from accidentally re-flattening to BLDC-only.
- Refreshed Last activity.

## Notes

- Followed `--discuss` flow despite the missing `gsd-sdk`: read the stepper repo README first (`C:\Usersİ154\STM32_vscode_projects\invpend_stepper\README.md`), surfaced 4 gray areas via AskUserQuestion, captured user decisions in `260428-q02-CONTEXT.md`, then edited.
- User picked **Major restructure** for scope, **Keep title with body clarification** for the title question, **Medium contrast table** for stepper detail level, **Yes** for adding the stepper repo link.
- Minor inconsistency left as-is: the architecture-animation TODO brief (lines 88–100) still references "torque command" / "FOC" / "1 kHz cycle" — those numbers are mismatched (BLDC = 16 kHz, stepper = 1 kHz). Since it's a placeholder brief not published prose, left for Sander to refine when he creates the asset.
- Hardware-photo TODO brief (lines 65–74) still names "IHM08M1" specifically. Same reasoning — not published prose, refine when shooting.

## Verification

- Hugo not built locally as part of this change. The shortcode form, table syntax, and frontmatter are all consistent with what already worked in this file. Will render on the next push to `main` via the GitHub Actions workflow.
- Title wording unchanged → no URL/slug change.
