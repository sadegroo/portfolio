---
quick_id: 260428-q02
slug: dual-rig-rewrite
date: 2026-04-28
description: Major restructure of BLDC pendulum post to cover both stepper and BLDC hardware variants
mode: quick --discuss (manual — gsd-sdk not installed locally)
related: 260428-q01 (added the scoring figures this rewrite re-attributes)
---

# Quick Task 260428-q02: BLDC Post — Dual-Rig Rewrite

## Goal

Reframe `content/posts/bldc-pendulum/index.md` from "I built a BLDC rig" to "one framework, two rigs." The framework supports a stepper-motor rig (the stock STEVAL-EDUKIT01 setup) AND the BLDC rig — they share the kit frame, NUCLEO controller, pendulum encoder, and the entire Pi/Simulink/grading layer above the firmware. The just-added scoring figures from q01 are stepper-rig results and need explicit attribution.

## Decisions (from CONTEXT.md)

1. Scope: **Major restructure** (rewrite setup section, generalize architecture section, add contrast table).
2. Title: **Keep "One Simulink Model, Two Targets"** and clarify "Two Targets" = sim + hardware in the digital-twin section.
3. Stepper detail: **Medium contrast table** (motor / driver / control rate / command type / encoders / firmware repo).
4. Repos: **Add `invpend_stepper`** as a third bullet in "Try it / source"; relabel BLDC firmware bullet to "Firmware (BLDC variant)".

## Tasks

1. **Frontmatter**: add `stepper` to tags; rewrite summary to lead with the framework, mention both motor flavours.
2. **Intro paragraph**: change "brushless motor's shaft" → "motor shaft" so sentence one is rig-neutral.
3. **Setup section**: rename to "The setup: one kit, two rigs"; rewrite to lead with the shared kit, then introduce the two motor variants; add 6-row contrast table.
4. **Brain and muscle / STM32 paragraph**: generalize from BLDC-only to cover both variants (one sentence each: BLDC = 16 kHz FOC torque on IHM08M1; stepper = 1 kHz accel cmd, L6474 handles current).
5. **Digital twin / variant-subsystem paragraph**: add the title clarifier ("Those are the two targets in the title — *simulation* and *real hardware*…") and note the stepper-vs-BLDC choice is a second variant switch underneath.
6. **Teaching lab**: add "(stepper rig)" to both scoring-figure captions and alt text; lead the analysis paragraph with "two teams ran the stepper rig"; close with one sentence acknowledging the BLDC rig produces the same shape of plot.
7. **What it looks like running**: add parenthetical "(the BLDC variant, in this clip)" so readers know which rig the swing-up video shows (the existing "16 kHz commutating" line already pinned it implicitly).
8. **Try it / source**: split firmware bullet into BLDC + stepper variants; pluralize the build-instructions sentence.
9. **Bottom TODO list**: remove leaderboard from item 4 (done in q01); change "both" → "all three" in item 1.
10. **STATE.md**: add q02 row to Quick Tasks; record q01's commit hash (was `_pending_`); add a Decisions block capturing the dual-rig framing as a load-bearing decision so future edits don't re-flatten.
11. **Commit atomically**.

## Constraints

- Don't push (per CLAUDE.md).
- Keep tone (confident, concrete, lightly personal).
- Don't touch the swing-up video, the drafting notes at the top, or other commented placeholders beyond what's listed in task 9.
