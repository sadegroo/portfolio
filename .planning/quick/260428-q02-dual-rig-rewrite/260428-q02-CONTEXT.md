---
quick_id: 260428-q02
slug: dual-rig-rewrite
date: 2026-04-28
status: Ready for editing
---

# Quick Task 260428-q02: BLDC Post — Dual-Rig Rewrite — Context

## Task Boundary

The BLDC pendulum post (`content/posts/bldc-pendulum/index.md`) currently treats the rig as if it were BLDC-only. Reality is that the framework supports two hardware variants — a stepper-motor rig (the original STEVAL-EDUKIT01 setup with an L6474 driver) and a BLDC rig (the user's contribution with a maxon ECX FLAT 42 M on an IHM08M1). Both share the kit frame, the NUCLEO-F401RE, the 2400 CPR pendulum encoder, the Pi/Simulink/SPI/grading layer. They differ in motor, driver board, control rate, command interface (torque vs acceleration), and firmware repo. The just-added scoring figures are stepper-rig results — `scoring.txt` literally says "STEPPER LEADERBOARD".

## Implementation Decisions

### Scope
- **Major restructure.** Reframe the post from "I built a BLDC rig" to "one framework, two rigs." Setup section gets a meaningful rewrite. Architecture section gets generalized. ~80 line diff.

### Title
- **Keep the existing title** ("One Simulink Model, Two Targets") and clarify "Two Targets" in the body — specifically in the digital-twin section's variant-subsystem paragraph, where the meaning naturally lives. No title change.

### Stepper detail level
- **Medium: small contrast table** with motor / driver / control rate / command type / motor encoder / pendulum encoder / firmware repo across the two rigs. Lifts numbers from `invpend_stepper/README.md` (1 kHz acceleration cmd, L6474, X-NUCLEO-IHM01A1) and existing post (16 kHz FOC torque, IHM08M1).

### Repo links
- **Add `invpend_stepper`** as a third bullet in "Try it / source", with the same TODO comment about confirming visibility before publishing. Re-label the BLDC firmware bullet to "Firmware (BLDC variant)" so the parallel is explicit.

### Claude's Discretion
- Hero image and swing-up video: BLDC content. Will add a brief parenthetical "(BLDC rig in this clip)" to the swing-up section to disambiguate, since the existing "16 kHz commutating" line already pins that section to BLDC.
- Scoring figure captions: prefix with "(stepper rig)" — minimum-effort, clearest signal. Don't move/swap the figures.
- Frontmatter `summary` and `tags`: update to reflect dual-rig framing (add "stepper" tag; rewrite summary to lead with the framework not the BLDC drive).
- Intro paragraph (line 40): change "brushless motor's shaft" → "motor shaft" to make it rig-neutral; the dual-rig story is then introduced in the setup section, not crammed into sentence one.

## Specific Ideas

Replacement section anchors:
- `## The setup, in two minutes` → `## The setup: one kit, two rigs`
- Add contrast table after the setup prose, before the existing hardware-photo placeholder block
- STM32 paragraph in "Brain and muscle" gets one sentence per variant (BLDC: 16 kHz FOC torque; stepper: 1 kHz accel cmd, L6474 handles current)
- Variant-subsystem paragraph in "The digital twin" picks up a parenthetical noting the stepper-vs-BLDC choice is a *second* variant switch layered underneath
- Teaching-lab paragraph: add "two teams ran the stepper rig" so attribution is in the prose, not just the captions
- Captions on `scoring_theta.png` and `scoring_pi.png` get "(stepper rig)" prefix

## Canonical References

- `C:\Usersİ154\STM32_vscode_projects\invpend_stepper\README.md` — source of truth for stepper-rig hardware/firmware specs
- `content/posts/bldc-pendulum/scoring.txt` — confirms "STEPPER LEADERBOARD" attribution
- `content/posts/bldc-pendulum/index.md` — the file being edited
