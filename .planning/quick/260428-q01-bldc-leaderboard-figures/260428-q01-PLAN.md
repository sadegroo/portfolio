---
quick_id: 260428-q01
slug: bldc-leaderboard-figures
date: 2026-04-28
description: Add scoring figures (Theta + Pi) to BLDC post competition section
mode: quick (manual — gsd-sdk not installed locally)
---

# Quick Task 260428-q01: BLDC Leaderboard Figures

## Goal

Replace the commented-out `competition-leaderboard.png` placeholder in `content/posts/bldc-pendulum/index.md` with the two real per-team scoring plots (`scoring_theta.png`, `scoring_pi.png`) auto-generated by the MATLAB grading script. Add a short paragraph that lands the speed-vs-fidelity tradeoff: Team Theta was a second faster (4.68 s vs 5.74 s) but had nearly double the SMAPE (119 vs 68), and both teams still tied on total points.

## Source data

`content/posts/bldc-pendulum/scoring.txt` (leaderboard from grading script):

| Team       | Best swing-up | Best SMAPE | Time pts | SMAPE pts | Total | Rank |
|------------|---------------|------------|----------|-----------|-------|------|
| Team Theta | 4.681 s       | 119.26     | 2        | 1         | 4     | 1    |
| Team Pi    | 5.741 s       | 67.52      | 1        | 2         | 4     | 1    |

## Tasks

1. **Edit `content/posts/bldc-pendulum/index.md`** — replace the commented placeholder block (lines 125–136 in the pre-edit file: the `📷 COMPETITION RESULTS / LEADERBOARD` brief plus the commented `{{< figure src="competition-leaderboard.png" ... >}}` line) with two stacked `{{< figure >}}` shortcodes for `scoring_theta.png` and `scoring_pi.png`, followed by one paragraph framing the tradeoff. Match the existing tone (contractions, em-dashes, one number per sentence).
2. **Update `.planning/STATE.md`** — drop "📷 Competition leaderboard" from Open TODOs (the count was already off by one); add a `### Quick Tasks Completed` table; refresh "Last activity".
3. **Commit atomically** — single commit with the index.md edit + STATE.md + the two plan/summary docs under `.planning/quick/`.

## Constraints

- Don't push (per CLAUDE.md — user reviews locally).
- Don't touch the swing-up video, the TODO list at the top of the post, or other commented placeholders.
- Stacked figures, not side-by-side (PaperMod default; safest across viewport widths).
