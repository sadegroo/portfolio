+++
title = "Search"
layout = "search"
summary = "Search posts and pages"
url = "/search/"

# PaperMod recipe — needs:
#   1. outputs.home includes "JSON"  (we have this in hugo.toml)
#   2. This file, with layout = "search", so the menu link /search/ resolves
#      to the theme's search UI instead of 404ing.
# Body intentionally empty — the layout owns the page.
+++
